#!/usr/bin/env node

// Absolute minimal test - just log and exit immediately
console.error('IMMEDIATE: Script started');
console.error('IMMEDIATE: Node version:', process.version);
console.error('IMMEDIATE: Exiting immediately');
process.exit(0);